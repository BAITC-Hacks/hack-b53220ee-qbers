# .env IS committed (private repo). Personal overrides go in .env.local.
.env.local

# Python
.venv/
__pycache__/
*.pyc
backend/staticfiles/

# Node
node_modules/
frontend/dist/

# Logs / OS
logs/
*.log
.DS_Store
Thumbs.db

# Root package.json has no dependencies — lockfiles there are noise
/package-lock.json
/pnpm-lock.yaml

# Download cache for `manage.py import_transport` (the processed data lives in Postgres + db/init snapshot)
data/raw/
